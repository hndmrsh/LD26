#pragma strict

import System.Collections.Generic;

public class GlobalScriptEvents extends ScriptEvents {

	function ExecuteEvent(event : String) : boolean {
		switch(event){
		
		}
		
		return false;
	}
}